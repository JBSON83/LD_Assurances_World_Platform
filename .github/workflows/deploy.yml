name: Deploy to Hostinger VPS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST_IP }}
          username: ${{ secrets.HOST_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          timeout: 60s
          command_timeout: 20m
          debug: true
          script: |
            echo "🚀 Démarrage du déploiement sur Hostinger VPS $(hostname)..."
            
            # Libération AGGRESSIVE du port 80
            echo "🧹 Libération agressive du port 80..."
            sudo systemctl stop nginx &> /dev/null || true
            sudo systemctl stop apache2 &> /dev/null || true
            sudo fuser -k 80/tcp &> /dev/null || true
            sudo lsof -t -i:80 | xargs sudo kill -9 &> /dev/null || true
            
            # Nettoyage Docker
            echo "🧼 Nettoyage système Docker..."
            docker system prune -af --volumes || true
            
            # Vérification Docker
            if ! command -v docker &> /dev/null; then
              echo "📦 Installation Docker..."
              curl -fsSL https://get.docker.com | sh
            fi
            
            if ! command -v docker-compose &> /dev/null; then
              echo "📦 Installation docker-compose..."
              apt-get update && apt-get install -y docker-compose || pip install docker-compose
            fi

            # Gestion du dépôt Git
            PROJECT_DIR="$HOME/LD_Assurances_World_Platform"
            if [ ! -d "$PROJECT_DIR" ]; then
              echo "📥 Clonage du dépôt..."
              git clone https://github.com/JBSON83/LD_Assurances_World_Platform.git "$PROJECT_DIR"
            else
              echo "🔄 Mise à jour du code..."
              git -C "$PROJECT_DIR" fetch --all
              git -C "$PROJECT_DIR" reset --hard origin/main
            fi
            
            cd "$PROJECT_DIR"
            
            # Lancement Docker Compose avec vérification
            echo "🐳 Reconstruction et lancement des containers..."
            docker-compose down --remove-orphans || true
            if docker-compose up -d --build; then
              echo "✅ Containers lancés. Vérification de l'état..."
              sleep 5
              docker-compose ps
              echo "🎉 Déploiement rétabli ! Site dispo sur http://jemassurance.online:8080 (port de secours)"
            else
              echo "❌ ÉCHEC du lancement Docker Compose."
              docker-compose logs --tail=50
              exit 1
            fi
